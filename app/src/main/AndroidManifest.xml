<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tp1clientandroid">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TP1ClientAndroid"
        tools:targetApi="31">

        <activity
            android:name=".ConnexionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/home"/>

        <activity
            android:name=".InscriptionActivity"
            android:label="@string/inscription_activity_title" />

        <activity
            android:name=".TaskConsultationActivity"
            android:label="@string/task_consultation_activity_title" />

        <activity
            android:name=".TaskCreationActivity"
            android:label="@string/task_creation_activity_title" />

    </application>

</manifest>
